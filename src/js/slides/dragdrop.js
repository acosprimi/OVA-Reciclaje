/* ============================================
   SLIDE: Drag & Drop Activity - OVA Reciclaje
   ============================================ */

window.OVA = window.OVA || {};
OVA.slides = OVA.slides || {};

OVA.slides.dragdrop = (function() {
  function init() {
    OVA.dragdrop.init();
  }

  return { init };
})();

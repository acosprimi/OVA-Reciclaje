/* ============================================
   SLIDE: Quiz - OVA Reciclaje
   ============================================ */

window.OVA = window.OVA || {};
OVA.slides = OVA.slides || {};

OVA.slides.quiz = (function() {
  function init() {
    OVA.quiz.init();
  }

  return { init };
})();

/* ============================================
   SLIDE: Exploration - OVA Reciclaje
   ============================================ */

window.OVA = window.OVA || {};
OVA.slides = OVA.slides || {};

OVA.slides.exploration = (function() {
  function init() {
    OVA.hotspots.init();
  }

  return { init };
})();
